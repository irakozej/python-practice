# Create a class Car with __init__() method that takes manufacturer & build_date as parameters & saves it as class variable to self.

class Car:
    def __init__(self, manufacturer, build_date):
        self.manufacturer = manufacturer
        self.build_date = build_date
